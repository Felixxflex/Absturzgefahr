
    <div class="container2">
        <div class="form-wrapper">
            <div class="form-title">
                <h3>Contact us and explain what service you are interested in!</h3>
            </div>
            <div class="form-content">
                <%= form_for @contact do |f| %>
                    <div class="col-md-6">
                        <%= f.label :name %></br>
                        <%= f.text_field  :name, required: true, class: "contact-form-text-area", placeholder: "Full Name"%></br>

                        <%= f.label :email %></br>
                        <%= f.text_field :email, required: true, class: "contact-form-text-area", placeholder: "Email" %></br>

                        <%= f.label :message %></br>
                        <%= f.text_area :message, rows: 8, cols: 40, required: true, class: "contact-form-text-area", 
                                placeholder: "Send me a message"%></br>

                        <div class= "d-none">
                            <%= f.label :nickname %>
                            <%= f.text_field :nickname, :hint => 'Leave this field blank!' %>
                        </div>

                        <%= f.submit 'Send Message', class: 'btn btn-danger' %>
                    </div>
                <% end %>
            </div>
    </div>
</div>




